# Environment audit — Phase 0

Audited 2026-09-18 from Phil's workstation over SSH. All commands read-only unless noted.
Host column: **gpu** = `ssh gpu` (VM `k8s-gpu-1`, Proxmox VMID 102), **pve** = `ssh pve` (Proxmox host, 192.168.1.10).

## Blockers found during the audit (all resolved or decided during Phase 0)

| # | Finding | Host | Resolution |
|---|---|---|---|
| 1 | `ssh pve` rejected the workstation key (`~/.ssh/id_ed25519`, `skynetDev`). | pve | Phil authorised the key. Verified with `qm list`. |
| 2 | `sudo` required a password for `hqadmin`. | gpu | Phil added passwordless sudo. `sudo -n true` passes. |
| 3 | Docker is not installed; runtime is containerd 2.3.3 + nerdctl 2.3.5. | gpu | **Decision: do not install Docker CE.** Forge uses `nerdctl compose` (requirements v2 updated). |
| 4 | GPU held (62.8 GB) by the `big-cat` voice stack (nerdctl, restart-unless-stopped). | gpu | Out of scope. Phil stops big-cat before Forge sessions. `forge-api` gets a free-VRAM guard (`FORGE_MIN_FREE_VRAM_GB`, default 80) that refuses and names the holders. |

Also: `~/.ssh/config` on the workstation is wrapped in a PowerShell here-string (`@" … "@ | Set-Content …`). ssh tolerates it; worth cleaning up.

## SSH targets

| Alias | HostName | User | Key | Status |
|---|---|---|---|---|
| `gpu` | `k8s-gpu-1` (192.168.1.51) | `hqadmin` | `~/.ssh/id_ed25519` | Works |
| `pve` | `192.168.1.10` | `root` | `~/.ssh/id_ed25519` (`IdentitiesOnly yes`) | Works (after Phil authorised the key) |

VMID 102 = `k8s-gpu-1` verified with `qm list` on pve.

## GPU VM (`k8s-gpu-1`, host: gpu)

### OS / hardware
- Ubuntu 24.04.3 LTS, kernel `6.8.0-110-generic`
- QEMU Q35 + ICH9 machine; 16 vCPU; 117 GiB RAM, no swap
- Rebooted once during Phase 0 (17:22 UTC) to prove the fstab entry. Boot note: host `nvidia-smi` reports "No devices were found" for the first few seconds after boot while the driver initialises; `/var/run/cdi/nvidia.yaml` is regenerated by `nvidia-cdi-refresh` at boot. Anything GPU-related started at boot must wait for both (big-cat's unit already does).
- NIC `enp6s18` 192.168.1.51/24 (DHCP); bridge `br-728d569dc27e` 10.4.1.1/24 with three veths (the nerdctl containers)

### GPU / driver (passthrough already verified by Phil — not re-checked)
- `NVIDIA RTX PRO 6000 Blackwell Max-Q Workstation Edition`, PCI `06:10.0`, 97,887 MiB
- Driver **580.126.09**, reports CUDA **13.0**
- Persistence mode Off; at audit: P8, 41 °C, 7 W, 0 % util, **62,825 MiB used** by:
  - PID 1964 `python3 server.py` — 5,014 MiB (Chatterbox TTS container)
  - PID 2834 `VLLM::EngineCore` — 57,796 MiB (vLLM, `Qwen/Qwen3.6-35B-A3B-FP8`, `--gpu-memory-utilization=0.6`)
- NVIDIA Container Toolkit **1.19.0** installed (`nvidia-container-toolkit`, `-base`, `libnvidia-container1`, `-tools`), `nvidia-ctk 1.19.0`; `nvidia-cdi-refresh.service` enabled (CDI spec at `/var/run/cdi/nvidia.yaml`)

### Container runtimes
| Component | State |
|---|---|
| Docker | **not installed** |
| containerd | v2.3.3, active, enabled; `/etc/containerd/config.toml` default runtime `runc` (no nvidia runtime entry; GPU access is via CDI) |
| nerdctl | 2.3.5 at `/usr/local/bin/nerdctl` (rootful; `hqadmin` has passwordless sudo) |
| GPU hello-world (`sudo nerdctl run --rm --gpus all nvidia/cuda:12.8.0-base-ubuntu24.04 nvidia-smi`) | **PASS** (exit 0) before and after the Phase 0 reboot; container sees `NVIDIA RTX PRO 6000 Blackwell Max-Q Workstation Edition`, driver 580.126.09. Image is now cached on the VM. |

### Kubernetes
- kubelet **v1.29.15** is `active` and `enabled` (kubeadm drop-in), but it **cannot reach its control plane** `192.168.1.52:6443` (`no route to host`, "Unable to register node", repeating since boot). The node is not registered and has **no pods**: all three containerd shims are in the containerd `default` namespace (nerdctl), none in `k8s.io`.
- `kubectl` has no kubeconfig for hqadmin (`~/.kube` missing).
- Conclusion: **no Kubernetes workloads are running.** The control plane VM `k8s-cp-1` (VMID 103, 192.168.1.52) is stopped on Proxmox, so kubelet is an orphaned agent that fails to register every few seconds. Left running per instructions; disabling it is Phil's call (§1 says Kubernetes stays shut down while Forge runs).

### Other services on the VM (all out of scope for Forge, untouched)
- `big-cat-stack.service` (enabled, currently `inactive` — its `WorkingDirectory=/home/youruser/big-cat/deploy` is a placeholder path, so the unit itself likely fails at boot). The containers nonetheless survive reboot via containerd's `restart: unless-stopped` policy. Compose file: `/home/hqadmin/big-cat/deploy/compose.yaml`. Containers: `vllm` (:8000), `stt` speaches (:8001), `tts` chatterbox (:8002). All three request `count: all` GPUs.
- `ollama.service` active, enabled, listening on 127.0.0.1:11434 (no VRAM held at audit).
- Listening: 22, 8000, 8001, 8002, 10250 (kubelet), 10248, 11434, 44435 (localhost).

### Disks and mounts (after Phase 0)
```
NAME     SIZE TYPE FSTYPE LABEL MOUNTPOINT  UUID
sda      300G disk                          (Proxmox scsi0, local-zfs:vm-102-disk-1)
├─sda1     1G part vfat         /boot/efi   BC31-C9C9
└─sda2 298.9G part ext4         /           f0eca0f1-e0f7-4134-aab6-d3e3799bc5fb
sdb      400G disk                          (Proxmox scsi1, local-zfs:vm-102-disk-2, added Phase 0)
└─sdb1   400G part ext4   forge /srv/forge  a44b7035-2d9a-419f-a3ee-e20580c4a3cc
sr0     1024M rom
```
- Root: 294 G, 210 G used, **69 G free (76 %)**. Untouched by Phase 0 apart from the cached CUDA base image.
- **`/srv/forge`**: 393 G free (ext4, `-m 0`, GPT, label `forge`). fstab: `UUID=a44b7035-2d9a-419f-a3ee-e20580c4a3cc /srv/forge ext4 defaults,nofail 0 2`. Verified with `mount -a` and a reboot. Layout dirs `models/ db/ profiles/ jobs/` created, owned by `hqadmin`.
- containerd's data root stays at `/var/lib/containerd` on the root disk (Phase 0 item 3). Forge compose will use bind mounts under `/srv/forge` rather than named volumes, so nothing else lives on root.
- Backup one-liner (from the workstation, run when convenient): `rsync -a --delete gpu:/srv/forge/jobs gpu:/srv/forge/db <backup-dest>/` (jobs and db are the only irreplaceable data).

### Tooling
- tmux **3.4** present (§2 tmux rule usable).
- apt sources: `kubernetes.list`, `nvidia-container-toolkit.list`, `ubuntu.sources`. No Docker repo.

## Proxmox host (host: pve) — audited after key fix

- `pve-manager/9.0.3`, kernel `6.14.8-2-pve`. SSH as root at 192.168.1.10 now works.
- `qm list`: **VMID 102 = `k8s-gpu-1`, running** (verified). Other VMs: 100 `ai-vm` (stopped), 101 `svc-influxdb-vm`, 103 `k8s-cp-1` (stopped — explains kubelet's unreachable control plane at 192.168.1.52), 104 `k8s-w-1` (stopped), 105 `gattonehq-edge`, 108 `homeassistant`.
- `qm config 102` (relevant lines): `scsihw: virtio-scsi-single`; `scsi0: local-zfs:vm-102-disk-1,cache=writeback,iothread=1,size=300G`; `efidisk0: local-zfs:vm-102-disk-0`; `hostpci0: 0000:81:00.0` (the GPU — not touched); `agent: 1`; `parent: phase-09c-openwebui-vllm-working` (VM has snapshots). **`scsi1` is free.**
- `pvesm status`:

| Name | Type | Total | Used | Available |
|---|---|---|---|---|
| `local` | dir | 1.35 TiB | 3.2 GiB | 1.34 TiB |
| `local-zfs` | zfspool | 1.68 TiB | 339 GiB | 1.34 TiB |

  Both back onto the same ZFS pool (`local` is the directory dataset, `local-zfs` the zvol pool). The existing VM disks are on `local-zfs`. Chosen for the 400 GB data disk: **`local-zfs`** (zvol, same as `scsi0`; 1.34 TiB free, thin-provisioned).

## Caddy (LXC 106 `caddy` on pve, 192.168.1.2)
- Caddy **v2.11.2**, unprivileged LXC, rootfs `local-zfs:subvol-106-disk-0` (4G).
- Config is a single file: **`/etc/caddy/Caddyfile`** inside the LXC. No `import` directory; each hostname is a top-level site block. Global block uses `acme_dns cloudflare {env.CF_API_TOKEN}` for certs, so new `*.gattonehq.com` hostnames only need a site block plus a DNS record.
- Existing pattern for plain HTTP backends (e.g. `influxdb.gattonehq.com { reverse_proxy 192.168.1.50:8086 }`); `gpu.gattonehq.com` already proxies to `https://192.168.1.51` (the Forge VM) with `tls_insecure_skip_verify`.
- To add a route (Phil does this, on pve): `pct exec 106 -- sh -c 'cat >> /etc/caddy/Caddyfile'` with a block like `forge.gattonehq.com { reverse_proxy 192.168.1.51:<FORGE_PORT> }`, then `pct exec 106 -- systemctl reload caddy`. LAN/Tailscale-only restriction is a Phase 3 concern (Caddyfile currently has no IP allow-lists).

## Repo / GitHub
- Remote `origin` = `https://github.com/Gattone78/asset-forge.git` (HTTPS, not the SSH alias mentioned in §1). `gh` is logged in as `pgattone` with **WRITE** permission on the repo. Default branch `main`.
- Working tree had an uncommitted 2-line edit to `docs/requirements.md` at session start.

## Phase 1 additions (2026-09-18)

- **Forge BuildKit daemon:** `forge-buildkit.service` (enabled), OCI worker, root `/srv/forge/buildkit`, socket `/run/forge-buildkit/buildkitd.sock`. The system `buildkit.service` (used by big-cat) is untouched. Build with `deploy/build.sh`.
- **Image:** `forge/comfyui:0.1.0` (8.8 GB) in containerd on the root disk. ComfyUI v0.36.0 @ `ee71d5c4`, torch 2.14.0+cu130, ComfyUI-UniRig @ `69ee59dc`. Full pins in `docs/phase-1.md`.
- **Container:** `forge-comfyui-1`, port 8188 on the LAN (`http://192.168.1.51:8188`), `restart: "no"`, never started at boot. Bind mounts: `/srv/forge/models`, `/srv/forge/comfy/{output,input,temp,user,ce}`.
- **UniRig isolated env:** 12 GB pixi env at `/srv/forge/comfy/ce/envs/unirig-nodes` (torch 2.8.0+cu128, flash_attn, spconv, bpy). Created by `python install.py` in the container; must exist before the UniRig nodes register.
- **Disk after Phase 1:** `/srv/forge` 87 GB used / 307 GB free (models 49 GB, UniRig env 12 GB, BuildKit cache 26 GB). Root 104 GB free after the container is stopped (46 GB at the low point during builds).
- **Models on disk:** see the table in `docs/phase-1.md`. `HF_TOKEN` not required for anything downloaded so far.
- **Measured VRAM (peak / resident):** FLUX.1 schnell bf16 35.8 / 33 GB; TRELLIS.2 bf16 @1024 ≈ 19 / 13.7 GB; UniRig 5.4 / 3.0 GB; container idle 560 MiB; stopped 2 MiB. Informs `FORGE_MIN_FREE_VRAM_GB` (default 80 is comfortable for one model at a time).
- **Attach commands used:** `ssh gpu`, then `tmux attach -t forge` (builds / installs) or `tmux attach -t forge-dl`, `forge-dl2`, `forge-dl3` (model downloads; all finished and gone).

## Phase 2 additions (2026-09-24)

- **forge-api** runs on the host as `hqadmin`: `forge-api.service` (enabled; boot-ordered after `nvidia-cdi-refresh`), `EnvironmentFile=/srv/forge/.env`, port **8080** on the LAN (`http://192.168.1.51:8080`, `/health`, `/viewer?job=<id>`). Node 22.23.2 at `/srv/forge/tools/node` (tarball, not apt). Sudoers scope in `/etc/sudoers.d/forge`.
- **svc-post** image `forge/svc-post:0.1.0` (Blender 4.5.14 + gltf-transform 4.5.0), run per job with `--user 1000:1000`.
- **Data:** `/srv/forge/jobs/<id>/{request.json,log.txt,refs/,raw/,out/}` (~630 MB per creature job, mostly `raw/`), `/srv/forge/db/forge.sqlite` (WAL), `/srv/forge/profiles/` (runtime copy), `/srv/forge/bakeoff/` (Phase 2 experiments, disposable), `/srv/forge/comfy/{input,output}/jobs/` (transient).
- **Backups (req 9):** `rsync -a --delete gpu:/srv/forge/jobs gpu:/srv/forge/db <dest>/`.
- **Idle behaviour verified:** comfyui stops `FORGE_GPU_IDLE_TIMEOUT` (600 s) after the last job; VRAM 2 MiB after.
- **Outage 2026-09-18 20:02 UTC:** guest journal ends abruptly (no OOM or hung-task record); Proxmox was taken down by Phil afterwards; VM back 2026-09-24 16:53 UTC. Two boot-time job failures (`nvidia-smi` not ready) led to the boot gate in forge-api.

## Phase 3 additions (2026-09-24)

- **Review UI** at `http://192.168.1.51:8080/ui/` (static Nuxt build served by forge-api; `/` redirects there). Rebuild on the VM with `./deploy/build-ui.sh` (npm install + nuxt generate + forge-api restart — only between jobs). Build inputs: `forge-ui/node_modules` (~600 packages) and output `forge-ui/.output/public` (5 MB) live in the VM checkout, not on the root disk.
- **Caddy route** for `forge.gattonehq.com` → `192.168.1.51:8080` with a LAN + Tailscale `remote_ip` allow-list: snippet in `docs/phase-3.md`, applied by Phil in LXC 106.
