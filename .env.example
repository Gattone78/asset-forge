# Copy to /srv/forge/.env on the VM (never commit .env). Read by forge-api's systemd unit.
HF_TOKEN=
FORGE_DATA=/srv/forge
FORGE_REPO=/srv/forge/src/asset-forge
FORGE_PORT=8080
FORGE_COMFY_URL=http://127.0.0.1:8188
FORGE_GPU_IDLE_TIMEOUT=600
FORGE_MIN_FREE_VRAM_GB=80
